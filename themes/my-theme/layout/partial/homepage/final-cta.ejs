<section class="newsletter-final-cta" aria-labelledby="newsletter-final-title">
    <div class="container">
        <p class="newsletter-eyebrow"><%= homepage.final_cta.eyebrow %></p>
        <h2 id="newsletter-final-title"><%= homepage.final_cta.title %></h2>
        <p><%= homepage.final_cta.body %></p>
        <%- partial('partial/homepage/newsletter-form', {
            placement: 'homepage_final',
            formId: 'pills-final',
            buttonLabel: 'Send me the Pills'
        }) %>
    </div>
</section>
