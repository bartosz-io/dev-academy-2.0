{
  "name": "wsa-2.0",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel ./src/index.html --open",
    "clean": "gulp clean",
    "build": "gulp && parcel build ./src/index.html --no-cache --no-source-maps --dist-dir ./../../source/web-security --public-url /web-security"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.2",
    "del": "^6.0.0",
    "gulp": "^4.0.2",
    "parcel": "^2.3.2",
    "posthtml-include": "^1.7.2",
    "posthtml-modules": "^0.8.0"
  }
}
